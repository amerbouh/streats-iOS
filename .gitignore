# macOS temporary files that should never be committed
.DS_Store
*.swp
profile

# Semi-personal settings, often included in workspaces
xcuserdata

# The spot where deprecated classes get moved. Since we using source-control, we do not want this feature
*.moved-aside

# Xcode private settings (window sizes, bookmarks, breakpoints, custom executables, smart groups)
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
#    NB: also, whitelist the default ones, some projects need to use these
!default.pbxuser
!default.mode1v3
!default.mode2v3
!default.perspectivev3

# CocoaPods
/Pods